
- O Collections é uma Biblioteca conhecida por High-Performance Container Datetypes.
- Trás um conjunto de estrutura de dados adicionais além das já presentes na biblioteca padrão. 
- As estruturas do `Collections` também são altamente eficientes na manipulação e armazenamento de dados.
- 

# 📌 Counter

O Counter recebe um iterável como parâmetro e cria um objeto do tipo Collections Counter que é parecido com um dicionário, contendo como chave o elemento da lista passada como parâmetro e como valor a quantidade de ocorrências desse elemento. 
#### Utilizando o Counter

```python
from collections import Counter
nome = 'gabrielle soares'

contador = Counter(nome)

print(contador)

# Output:
# Counter({'e': 3, 'a': 2, 'r': 2, 'l': 2, 's': 2, 'g': 1, 'b': 1, 'i': 1, ' ': 1, 'o': 1})
```


```python
from collections import Counter

texto = """ By submitting this form, I agree that JetBrains s.r.o.
may process the personal data I provided above and my
location for the purpose explained above and may engage third parties
in such processing. The consent can be revoked in my profile at any time.
More details about the processing are in JetBrains Privacy Notice.
In addition, an unsubscribe link is included in each email."""


palavras = texto.split()
print(Counter(palavras))

#Output:
# Counter({'in': 4, 'the': 3, 'I': 2, 'JetBrains': 2, 'may': 2, 'above': 2, 'and': 2, 'my': 2, 'By': 1, 'submitting': 1, 'this': 1, 'form,': 1, 'agree': 1, 'that': 1, 's.r.o.': 1, 'process': 1, 'personal': 1, 'data': 1, 'provided': 1, 'location': 1, 'for': 1, 'purpose': 1, 'explained': 1, 'engage': 1, 'third': 1, 'parties': 1, 'such': 1, 'processing.': 1, 'The': 1, 'consent': 1, 'can': 1, 'be': 1, 'revoked': 1, 'profile': 1, 'at': 1, 'any': 1, 'time.': 1, 'More': 1, 'details': 1, 'about': 1, 'processing': 1, 'are': 1, 'Privacy': 1, 'Notice.': 1, 'In': 1, 'addition,': 1, 'an': 1, 'unsubscribe': 1, 'link': 1, 'is': 1, 'included': 1, 'each': 1, 'email.': 1})
```

- Bom para analisar palavras que se repetem 


# 📌 Default Dict

Diferente do dicionário tradicional, o `defaultdict` aceita uma função que será chamada quando um valor não existir.

- Ao criar um dicionário utilizando-o, será criado um valor Default.
- Pode utilizar lambda para isso. 
- Este valor será utilizado sempre que não houver um valor definido.
- Caso tentemos acessar uma chave que não, existe, essa chave será criada e o valor default será atribuído.

>⚠️ 
> Lambdas são funções sem nome, que podem ou não receber parâmetros de entrada e retornar valores

#### Exemplo KeyError
```python
dicionario = {'nome': 'Gabrielle'}

print(dicionario)
print(dicionario['nome'])
print(dicionario['sobrenome']) #KeyError

```

#### Exemplo defaultdict com Lambda
```python
from collections import defaultdict

dicionario = defaultdict(lambda:1)
dicionario['curso'] = 'Geek'
print(dicionario)
print(dicionario['outro'])
print(dicionario)

#Output:

#defaultdict(<function <lambda> at 0x7f530a9231a0>, {'curso': 'Geek'})
#1
#defaultdict(<function <lambda> at 0x7f530a9231a0>, {'curso': 'Geek', 'outro': 1})

```

Neste exemplo, o dicionario utiliza a função `lambda:1` como padrão para os valores que não foram inicializados previamente. 


# 📌 Ordered Dict

Enquanto o dicionário padrão do Python não garante uma ordem específica, o `OrderedDict` lembra a ordem de inserção dos elementos.

- A ordem dos elementos no `OrderedDict` é preservada de acordo com a ordem de inserção.

> Isso é útil em diversas situações em que é necessário iterar sobre os itens do dicionário de acordo com a ordem de inserção.

```python
# Diferença entre Dict e Ordered Dict

#Dict
dict1 = { 'a': 1, 'b': 2}
dict2 = { 'b': 2, 'a': 1}
print(dict1 == dict2) # São iguais porque a ordem não importa
# Output: True
  

# Ordered Dict
from collections import OrderedDict
odict1 = OrderedDict({ 'a': 1, 'b': 2})
odict2 = OrderedDict({ 'b': 2, 'a': 1} )
print(odict1 == odict2) # Não são iguais porque a ordem importa
# Output: False

```

# 📌  Named Tuple

Um subtipo de tupla que atribui nomes fixos aos elementos.

Com o `namedtuple`, podemos criar uma classe de tupla personalizada sem a necessidade de criar uma classe completa.

##### Exemplo
```python
from collections import namedtuple

Pessoa = namedtuple('Pessoa', ['nome', 'idade'])
p1 = Pessoa('João', 25)
print(p1.nome)  # João
print(p1.idade)  # 25
```

> Aqui, criamos um `namedtuple` chamado `Pessoa` com os campos `nome`e `idade`.
> Em seguida, instanciamos esse tipo de dado e atribuímos valores aos seus campos.
   Por fim, podemos acessar esses campos como atributos da instância.

### Modos de declarar

#### Modo 1
```python
cachorro = namedtuple('cachorro', 'idade raca nome')
```

#### Modo 2
```python
cachorro2 = namedtuple('cachorro', 'idade, raca, nome')

```

#### Modo 3
```python
cachorro3 = namedtuple('cachorro', ['idade', 'raca', 'nome'])
```

###  Modos de utilizar

#### Forma 1 
```python
ray = cachorro(idade=2, raca= 'Chow-CHow', nome='Ray')
```

#### Forma 2 
```python
jose = cachorro3(6, 'Vira Lata', 'Jose')
```

### Modos de acesso

#### Forma 1
```python
print(ray[0])
print(ray[1])
print(ray[2])
```

#### Forma 2
```python
print(jose.idade)
print(jose.raca)
print(jose.nome)
```


# 📌 Deque

Um tipo de estrutura de dados frequente na programação é a fila, que segue o princípio **FIFO** (_First In, First Out_).

O pacote `Collections` oferece a classe `deque`, que permite criar uma fila com todas as operações de inserção e remoção eficientes.